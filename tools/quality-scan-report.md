# Quality scan report

Generated: 2026-04-22T21:15:05.714Z  

Total questions: **771**  

Questions with issues: **66** — ERROR 46, WARN 9, INFO 16


## By level

| Level | Count | Errors | Warns | Info |
|---|---:|---:|---:|---:|
| U7 / Initiation | 40 | 0 | 0 | 0 |
| U9 / Novice | 275 | 0 | 0 | 12 |
| U11 / Atom | 120 | 0 | 0 | 2 |
| U13 / Peewee | 118 | 44 | 0 | 0 |
| U15 / Bantam | 114 | 2 | 6 | 2 |
| U18 / Midget | 104 | 0 | 3 | 0 |

## Top issue codes

| Code | Count |
|---|---:|
| pos-shape | 44 |
| dup-concept-in-age | 10 |
| mc-dup-opt | 7 |
| age-vocab-complex | 6 |
| dup-sit-in-age | 2 |
| dup-id | 2 |

## All flagged questions


### U9 / Novice

#### `u7q67` (mc)
- **INFO** `dup-concept-in-age` — same concept "Get back" as u7q14

#### `u9q3` (mc)
- **INFO** `dup-concept-in-age` — same concept "Open ice" as u7q7

#### `u9q8` (mc)
- **INFO** `dup-concept-in-age` — same concept "Backchecking" as u7q6

#### `u9q10` (mc)
- **INFO** `age-vocab-complex` — contains vocabulary likely too advanced for U9 / Novice

#### `u9q12` (mc)
- **INFO** `dup-concept-in-age` — same concept "Rebounds" as u7g4

#### `u9q26` (mc)
- **INFO** `age-vocab-complex` — contains vocabulary likely too advanced for U9 / Novice

#### `u9q30` (mc)
- **INFO** `age-vocab-complex` — contains vocabulary likely too advanced for U9 / Novice

#### `u9q34` (mc)
- **INFO** `dup-concept-in-age` — same concept "One-timer" as u7q88

#### `u9q53` (mc)
- **INFO** `age-vocab-complex` — contains vocabulary likely too advanced for U9 / Novice

#### `u9q78` (mc)
- **INFO** `age-vocab-complex` — contains vocabulary likely too advanced for U9 / Novice

#### `u9q88` (mc)
- **INFO** `age-vocab-complex` — contains vocabulary likely too advanced for U9 / Novice

#### `u9rink8` (rink)
- **INFO** `dup-concept-in-age` — same concept "Help your teammate" as u7q32


### U11 / Atom

#### `u11rink28` (rink)
- **INFO** `dup-concept-in-age` — same concept "F1 angle" as u11rink20

#### `u11rink36` (rink)
- **INFO** `dup-concept-in-age` — same concept "Backcheck lane" as u11rink8


### U13 / Peewee

#### `u13q1` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q2` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q3` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q9` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q12` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q14` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q15` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q16` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q18` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q19` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q21` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q24` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q26` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q29` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q31` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q34` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q37` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q40` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q41` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q43` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q44` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q46` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q47` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q51` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q54` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q57` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q59` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q61` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q64` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q66` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q68` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q74` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q76` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q80` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q81` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q82` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q84` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q86` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q92` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q94` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q95` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q96` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q97` (mc)
- **ERROR** `pos-shape` — pos must be an array

#### `u13q98` (mc)
- **ERROR** `pos-shape` — pos must be an array


### U15 / Bantam

#### `u15q9` (mc)
- **WARN** `mc-dup-opt` — option 4 duplicates an earlier option: "Play defensively and focus on preventing the scoring chance."

#### `u15q24` (mc)
- **WARN** `mc-dup-opt` — option 3 duplicates an earlier option: "Play defensively and focus on preventing the scoring chance."
- **WARN** `mc-dup-opt` — option 4 duplicates an earlier option: "Play defensively and focus on preventing the scoring chance."

#### `u15g1` (mc)
- **WARN** `mc-dup-opt` — option 3 duplicates an earlier option: "Stay centered and react — don't anticipate the play."

#### `u15tf1` (tf)
- **WARN** `dup-sit-in-age` — sit almost identical to u15tf1 in same age
- **INFO** `dup-concept-in-age` — same concept "Pinching on rush" as u15tf1
- **ERROR** `dup-id` — duplicate id (also seen in U15 / Bantam)

#### `u15tf2` (tf)
- **WARN** `dup-sit-in-age` — sit almost identical to u15tf2 in same age
- **INFO** `dup-concept-in-age` — same concept "Gap vs speed" as u15tf2
- **ERROR** `dup-id` — duplicate id (also seen in U15 / Bantam)


### U18 / Midget

#### `u18q21` (mc)
- **WARN** `mc-dup-opt` — option 4 duplicates an earlier option: "Play the same way regardless of the game score or time."

#### `u18q64` (mc)
- **WARN** `mc-dup-opt` — option 4 duplicates an earlier option: "Dump and change instead of managing the puck in transition."

#### `u18g13` (mc)
- **WARN** `mc-dup-opt` — option 4 duplicates an earlier option: "Stay centered and react — don't anticipate the play."
