import {writeFileSync,existsSync} from 'node:fs';
import {join} from 'node:path';
import {projectRoot,readJson} from './experimental-bank-files.mjs';
import {questionContentHash} from './question-batch-core.mjs';
const directory=join(projectRoot,'docs/factory/research/question-review/followup');
const rows=['mixed','u13'].flatMap(lane=>readJson(join(directory,`${lane}-proposals.json`)).entries);
const bank=['u7','u9','u11','u13','u15','u18'].flatMap(age=>readJson(join(projectRoot,`src/one-on-one/experimental-bank/${age}.json`)));
const source=new Map(bank.flatMap(s=>s.questions.map(q=>[q.id,{s,q}])));
const changes=readJson(join(directory,'../repairs/root-early-repairs.json')).changes;
const checkPath=join(directory,'u9-006-recheck.json'),check=existsSync(checkPath)?readJson(checkPath):null;
const checkRows=check?.results||(check?.questionId?[check]:[]);
if(rows.length!==55||new Set(rows.map(r=>r.questionId)).size!==55)throw Error('Expected55 unique follow-up records');
for(const row of rows){const {s,q}=source.get(row.questionId),change=changes.find(c=>c.questionId===q.id);if(questionContentHash(s,change?.before||q)!==row.contentHash)throw Error(`Stale review ${q.id}`);}
const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const question=q=>`<p>${esc(q.prompt)}</p>${q.options?`<ul>${q.options.map(o=>`<li>${esc(o.text)}</li>`).join('')}</ul>`:''}<p>${esc(q.explanation)}</p>`;
const output=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>RinkReads · Luna follow-up</title><style>body{font:16px/1.55 system-ui;background:#0c1a2e;color:#edf2f7;margin:0}main{max-width:960px;margin:auto;padding:30px 20px}h1,h2{font-family:Georgia,serif;font-weight:500}h1{font-size:44px;line-height:1.12}.kicker{color:#e0b95c;font-size:12px;letter-spacing:.15em}article{border:1px solid #aec5dd33;border-radius:16px;padding:22px;background:linear-gradient(135deg,#c3dcec10,#c3dcec03);margin:20px 0}small{overflow-wrap:anywhere;color:#aebdd1}.two{display:grid;grid-template-columns:1fr 1fr;gap:24px}a{color:#e0b95c}summary{cursor:pointer;padding:10px 0}@media(max-width:650px){.two{grid-template-columns:1fr}h1{font-size:34px}}</style><main><p class="kicker">RINKREADS / LUNA COACHING REVIEW</p><h1>54 questions retained.<br>One clearer pickup question.</h1><p>All55 earlier teaching-design flags received a complete scenario-context follow-up. The reviewers retained54 scoped planning routines. The U9 pickup question now teaches repeated checks for pressure and support during the approach.</p><p>This is an AI coaching review, separate from human coach approval. <a href="../repair-samples.html">Review the repair samples and leave feedback ↗</a></p>${rows.sort((a,b)=>(a.decision==='keep')-(b.decision==='keep')||a.questionId.localeCompare(b.questionId)).map(r=>{const {s,q}=source.get(r.questionId),change=changes.find(c=>c.questionId===q.id),verified=checkRows.some(c=>c.questionId===q.id&&c.contentHash===questionContentHash(s,q)&&(c.status==='pass'||c.verdict==='retain'));return `<article><p class="kicker">${esc(s.ageBand)} · ${change?verified?'Repaired · independently rechecked':'Repaired · recheck pending':'Retain'}</p><h2>${esc(s.title)}</h2><small>${esc(q.id)}</small><p>${esc(change?.reason||r.reason)}</p>${change?`<div class="two"><section><h3>Before</h3>${question(change.before)}</section><section><h3>Repaired</h3>${question(q)}</section></div>`:`<details><summary>Read the retained question</summary>${question(q)}</details>`}<a href="http://localhost:5173/?arena=experimental&age=${encodeURIComponent(s.ageBand)}&scenario=${encodeURIComponent(s.id)}&question=${encodeURIComponent(q.id)}#practice-arena">Open the question on the rink ↗</a></article>`;}).join('')}<p>Historical follow-ups retain their original content hashes. Repairs and final checks have separate receipts.</p></main></html>`;
writeFileSync(join(directory,'review.html'),output);
console.log(JSON.stringify({reviewed:rows.length,retained:rows.filter(r=>r.decision==='keep').length,repaired:rows.filter(r=>changes.some(c=>c.questionId===r.questionId)).length}));
