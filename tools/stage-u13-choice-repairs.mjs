import fs from 'node:fs';import {readBankFiles} from './experimental-bank-files.mjs';import {questionContentHash} from './question-batch-core.mjs';
const root='docs/factory/coaching-panel/choice-quality-2026-09-06',path=root+'/staged-repairs.json';if(fs.existsSync(path))throw Error('Frozen draft exists. Make a new revision.');
const bank=readBankFiles().bank;
const drafts=[
 ['exp26-u13-002-q5','You planned a reverse to D2. Before release, an opponent moves into that passing line. Which option has directly changed?', ['A pass toward W','A pass toward C','The reverse to D2'],'The reverse line to D2 is now occupied. Compare other outlets or protect the puck while you reassess. This hypothetical does not establish that W or C is open.',['W is another option to assess, but the stated change concerns D2.','C is another option to assess, but its availability is not established.','The opponent has entered the planned reverse line.']],
 ['exp26-u13-003-q5','Gold 1 follows YOU inward and stays between YOU and D1. C has a separate clear line to D1. Which support option needs a new angle?', ['The pass from D1 to C','The pass from D1 to YOU','D1 keeping possession'],'The stated marker blocks the line to YOU. Reoffer at another angle while leaving C’s separate lane available. D1 still chooses whether to pass or retain the puck.',['The hypothetical explicitly leaves C’s line clear.','YOU need a different relationship to the marker to offer a clear line.','Retaining possession is a carrier decision, not the blocked support line.']],
 ['exp26-u13-006-q5','Gold 1 enters the line from D1 to YOU. F1 still has a clear line to D1. Which move helps offer two separate passing options?', ['Move beside F1 on the same passing line','Find a different angle from F1, outside Gold 1’s line','Stay behind Gold 1 as a deeper target'],'Seek a separate angle so D1 can compare YOU and F1. Moving beside F1 may crowd one line; staying behind Gold 1 leaves the stated obstruction. The new angle must be checked as pressure changes.',['This gathers both supporters onto one line rather than offering separate routes.','This addresses the named blocker while preserving a separate F1 option.','Depth alone does not remove the blocker from the stated line.']],
 ['exp26-u13-011-q5','Gold 1 follows your lateral move and blocks the shot line. D2 has a clear passing line and room to receive. Which option uses that available support?', ['Shoot from the current position','Carry toward Gold 1','Pass to D2'],'A pass to D2 uses the support explicitly available in this hypothetical. Another carry or a later shot can become useful if the picture changes. The static scene alone does not guarantee reception or a successful next play.',['The hypothetical specifies an occupied shot line.','A carry can sometimes change pressure, but does not use the available D2 support.','This uses the clear support line named in the prompt.']],
 ['exp26-u13-012-q5','F2 still has the puck. Gold 2 blocks your planned return-pass route. Which adjustment offers F2 a different passing line?', ['Continue along that same route','Move to a new support angle','Call for the puck from the same spot'],'Offer a new angle and check it against Gold 2. F2 can keep possession or choose another play; a return pass is not owed. A call alone does not change the geometry, and a new angle is not automatically clear.',['Continuing on the specified route does not yet address its blocker.','A new angle changes the line for F2 to assess.','Communication helps coordination but does not by itself move the line.']]
];
const changes=drafts.map(([id,prompt,texts,explanation,reasons])=>{const s=bank.find(s=>s.questions.some(q=>q.id===id)),before=s.questions.find(q=>q.id===id);const after={...before,prompt,explanation,options:before.options.map((o,i)=>({...o,text:texts[i]}))};const revised={...s,version:s.version+1,questions:s.questions.map(q=>q.id===id?after:q)};return {scenarioId:s.id,questionId:id,fromVersion:s.version,toVersion:revised.version,beforeHash:questionContentHash(s,before),afterHash:questionContentHash(revised,after),before,after,optionReasons:after.options.map((o,i)=>({optionId:o.id,reason:reasons[i]})),status:'draft-awaiting-independent-payload-review'};});
fs.writeFileSync(path,JSON.stringify({generatedAt:new Date().toISOString(),status:'not-applied',humanCoachApproval:false,scope:'Five U13 changed-cue questions. Explicit hypothetical conditions replace careless-action giveaway wording. These are read-and-update tasks, not assessments of executed skating or guaranteed pass outcomes.',changes},null,2)+'\n');
fs.writeFileSync(root+'/snapshot.json',JSON.stringify(bank.filter(s=>changes.some(c=>c.scenarioId===s.id)),null,2)+'\n');
console.log('Five exact-source drafts staged; source bank unchanged.');
