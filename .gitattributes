# The portable Claude package records byte-exact SHA-256 manifests.
# Preserve the generated files across Windows and Unix checkouts.
docs/factory/claude-project/** -text
# Frozen candidate manifests also record byte-exact SHA-256 values.
docs/factory/coaching-panel/choice-quality-rewrite-*/candidates.json text eol=lf
