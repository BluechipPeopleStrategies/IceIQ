import fs from 'node:fs';import sharp from 'sharp';
const root='docs/factory/scene-audit-2026-09-06',input=JSON.parse(fs.readFileSync(root+'/render-input.json'));const start=Number(process.argv[2]||0),limit=Number(process.argv[3]||20),kind=process.argv[4]||'phone';const tileW=360,tileH=480,columns=5;const rows=Math.ceil(limit/columns);const layers=[];for(let i=0;i<limit;i++){const s=input[start+i];if(!s)break;const p=root+'/captures/'+s.id+'-'+kind+'.png';if(!fs.existsSync(p))continue;const b=await sharp(p).resize(tileW-12,tileH-40,{fit:'contain',background:'#0b1827'}).png().toBuffer();const left=(i%columns)*tileW,top=Math.floor(i/columns)*tileH;layers.push({input:b,left:left+6,top:top+30});const svg=Buffer.from(`<svg width="${tileW}" height="30"><rect width="100%" height="100%" fill="#102137"/><text x="8" y="21" font-family="Arial" font-size="16" fill="#fff">${start+i+1}. ${s.id}</text></svg>`);layers.push({input:svg,left,top});}fs.mkdirSync(root+'/sheets',{recursive:true});await sharp({create:{width:tileW*columns,height:tileH*rows,channels:3,background:'#0b1827'}}).composite(layers).png().toFile(root+`/sheets/${kind}-${String(start).padStart(3,'0')}.png`);console.log(root+`/sheets/${kind}-${String(start).padStart(3,'0')}.png`);
