import fs from 'node:fs';import {isCoachRoutePoint} from '../src/one-on-one/coachRouteSurfaceInput.js';
const p=JSON.parse(fs.readFileSync('docs/factory/research/question-review/packet-18/proposed-repairs.json'));const dist=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y);const lineDist=(p,a,b)=>{const dx=b.x-a.x,dy=b.y-a.y,t=Math.max(0,Math.min(1,((p.x-a.x)*dx+(p.y-a.y)*dy)/(dx*dx+dy*dy)));return dist(p,{x:a.x+t*dx,y:a.y+t*dy});};
const evidence=p.packets[0].scenarios.map(row=>{const s=row.replacement,a=s.setup.actors.find(a=>a.id===s.focusActorId),q=s.questions.find(q=>q.type==='position'),d=s.setup.actors.find(a=>a.team==='away'),puck=s.setup.puck.owner?s.setup.actors.find(a=>a.id===s.setup.puck.owner):s.setup.puck;if(!isCoachRoutePoint(q.reference))throw Error(s.id);return {id:s.id,start:{x:a.x,y:a.y},reference:q.reference,referenceOnIce:true,defenderDistanceBefore:dist(a,d),defenderDistanceAfter:dist(q.reference,d),...(s.setup.puck.owner===a.id?{puckMovesWithCarrier:true}:{puckDistanceBefore:dist(a,puck),puckDistanceAfter:dist(q.reference,puck)})};});
evidence.push({id:'wall-claim',straightSideBoundaryY:12.954,oldReferenceWallDistance:12.954-8.5,newReferenceWallDistance:12.954-11.5,D1DistanceToOriginalPassingSegment:lineDist({x:16,y:7},{x:12,y:7},{x:19,y:8}),D2DistanceToOriginalPassingSegment:lineDist({x:23,y:1},{x:12,y:7},{x:19,y:8}),limits:'Route proximity is geometry, not a stick-reach or bank-bounce simulation.'});
fs.writeFileSync('docs/factory/research/question-review/packet-18/root-geometry.json',JSON.stringify(evidence,null,2)+'\n');console.log(JSON.stringify(evidence));
