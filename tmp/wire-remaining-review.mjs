import fs from 'node:fs';
for(const f of ['tools/audit-experimental-coaching.mjs','tools/audit-question-expansion.mjs']){let s=fs.readFileSync(f,'utf8');const marker="'packet-20-final-recheck.json'";if(!s.includes("'packet-21-final-recheck.json'"))s=s.replace(marker,marker+','+Array.from({length:14},(_,i)=>`'packet-${i+21}-final-recheck.json'`).join(','));fs.writeFileSync(f,s);}
const f='tools/build-question-review-summary.mjs';let s=fs.readFileSync(f,'utf8');s=s.replace(' Full evidence: [packet application receipt]', ' Packets 21–34 received further scene and question repairs, including roster counts, net-side relationships, collection targets and conditional passing feedback. Initial AI passes were overturned where root checks found concrete defects; see [batch adjudication](packets-21-34-review.md). Full evidence: [packet application receipt]');fs.writeFileSync(f,s);
