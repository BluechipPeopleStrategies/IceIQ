import fs from 'node:fs';for(let n=21;n<=24;n++){let p=JSON.parse(fs.readFileSync(`docs/factory/research/question-review/packet-${n}/proposed-repairs.json`));for(let x of p.packets[0].scenarios){let s=x.replacement;console.log(JSON.stringify({id:s.id,brief:s.briefing,actors:s.setup.actors.map(a=>[a.label,a.team,a.x,a.y]),puck:s.setup.puck,placements:s.questions.filter(q=>q.type==='position').map(q=>[q.id,q.prompt,q.reference,q.explanation])}));}}

