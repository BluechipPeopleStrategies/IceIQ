import fs from 'node:fs';for(let n=Number(process.argv[2]);n<=Number(process.argv[3]);n++){let p=JSON.parse(fs.readFileSync(`docs/factory/research/question-review/packet-${n}/proposed-repairs.json`));for(let x of p.packets[0].scenarios){let s=x.replacement;console.log('\n'+s.id);for(let q of s.questions.filter(q=>q.type!=='position'))console.log(JSON.stringify({id:q.id.split('-').at(-1),p:q.prompt,o:q.options?.map(o=>[o.id,o.text]),a:q.answer,e:q.explanation}));}}
