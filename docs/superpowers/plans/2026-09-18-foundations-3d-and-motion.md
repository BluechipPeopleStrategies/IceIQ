# Foundations 3D rink and motion refresh — implementation scope

Owner approved the first 3D rink and immediate use of suitable video-layout ideas, then requested a fresh pass instead of assuming historical materials remain current.

Use the existing React/Three stack. Keep the exact foundation diagrams and state machine; add depth around the surface drawing, restricted camera controls, an overhead view and an accessible 2D alternative. Preserve the original six-world artwork. Bring over dimensional framing, a clear focal object, restrained accents and short reduced-motion-aware transitions. Inspect current local sources and current official capabilities; no new dependencies or paid generation.

Verification: existing flow regressions, browser texture validation across all rink states, view controls without score writes, touch-sized layouts, reduced motion, graphics failure recovery and production build. Keep all changes local and regenerate the content pack fingerprint. Helmet/referee 3D remain subsequent samples, not claimed complete.
