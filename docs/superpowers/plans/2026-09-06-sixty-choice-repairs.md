# Apply the sixty reviewed question repairs

Owner authorized all sixty replacements after the 78-question review. Existing experimental status, question types and optional reflection policy remain. Source: `docs/factory/coaching-panel/choice-quality-remaining-04/root-adjudication.json` (60 revise, 18 retain).

1. Stage replacements in the existing 02–14 packet boundaries, at most five full scenes per packet. Change only authorized question IDs. Preserve setups, unrelated questions and source metadata. Bump each affected scene once per applied revision.
2. Have a separate reviewer examine each exact replacement and every option, using the canonical puck offset and named scene evidence. Preserve failed drafts and revisions. Existing qualification evidence is reused explicitly; it is not human coaching certification.
3. Root reconciles each final payload and runs the dry-run/apply guards. Refresh current catalogs and curriculum bindings after application. Retain historical reviews and hashes.
4. Verify all replacement answer mappings and source closure; inspect representative browser flows across age bands and response types. Run the applicable regression suite, production build and release boundary. Commit and deploy only cleared content.
5. Publish a local completion report and update the canonical task list. Show unresolved work honestly; do not describe review flags as applied repairs.

Rulings: reuse the existing isolated release checkout and already calibrated author/reviewer sessions because the active agent tree has no free fresh-agent slots. Author and reviewer remain separate. Existing owner authorization covers reviewed release; no new permission round is needed. The dedicated Hockey Authority role in the root checkout remains historically unqualified and is not being used as an approval credential. The local feedback inbox was empty at preflight.

No new paid model API, Supabase migration, production administration endpoint, new tactical-claim certification, scene movement, or bulk conversion to reflections is part of this repair.
