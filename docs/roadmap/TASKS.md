# RinkReads — Task List

**Last updated:** 2026-09-05 · Ready release: reviewed packets01–11 plus six-world player home, device-local spaced practice, U7/U9-only rink discovery and verified 3D-game fixes. 73 tests and production build passed. Coach/goals/training migration-dependent release remains held.

**Scope:** RinkReads **app build + content factory**. Launch/distribution (sending the beta, marketing) and the separate BlueChip business are **out of scope** — parked at the bottom. Priority = position in this list. A new idea that isn't here goes to the **Parking Lot** first, then gets promoted on purpose — that's the scope-creep guard.

**Branch:** `main`

**Notion mirror (view-only):** [open in Notion](https://app.notion.com/p/398c5405e7f68172b891e7b7a5aac5ea) · this file is the source of truth; the mirror is a one-way copy, re-synced from here.

---

## 🔵 NOW — active front (max 3)

**Current SGS implementation and next teaching work (owner direction, September 5):** complete and verify the current mixed-read/presentation release, then calibrate a U7 foundation catalog and one U11 causal family before bulk generation. Explanations are optional evidence. Named question focus can move between players; focus, control, selection and possession are separate. The bounded D1 reflection after a real pass is implemented separately from the original attacking branch; applying its new position into further branches remains open. The production design must support variable read graphs, simultaneous cues, and both Frozen and Continuous pacing from the start, independently of Learning/Challenge feedback. Add vocabulary, picture labels, matching/sorting, fill-in-the-blank, player-bag packing and dressing activities with age-appropriate access. Borrow game-based basketball/soccer/rugby relationships and deliberate constraints through hockey-specific sources. **Do not count delivery formats, seeds or shuffled answers as new tactical situations.** Plans: [factory blueprint](../superpowers/specs/2026-09-05-sgs-thousands-question-factory.md), [game-based lesson supplement](../superpowers/specs/2026-09-05-sgs-game-based-learning.md), [question-variety contract](../superpowers/specs/2026-09-05-sgs-question-variety.md). Continuous decisions, generalized named-role content such as D4 recovery, tactical evaluation and the U7 equipment catalog still require implementation/review.

- **Recover auth users that have no profile row (dead-end accounts).** Found 2026-08-02 while cleaning up after the account-creation incident. The render gate is `if (!profile) -> AuthScreen`, so an authenticated user whose `profiles` row is missing is bounced to the signup form, which then tells them the email is already registered. There is no way out from the UI. `loadUser`'s retry (hotfix `6718fe3`) covers the write/read race but still gives up after 5 attempts, so a genuinely profile-less account is still stranded. **Three exist in production right now** — `thomas@bluechip-people-strategies.com` (2026-06-12, Thomas's own work address), `donkey@gmail.com` (2026-07-09), and `rinkreads-crash-1783623581262@example.com` (2026-07-09, obviously a test artifact). This is the signature the signup deadlock (`f917def`) left behind, so any real user who hit it is in the same state and cannot recover by retrying. Deliberately NOT fixed by data surgery: back-filling means inventing a `role`, and deleting an account with Thomas's name on it is worse. The right fix is systemic — detect session-without-profile and route to a short "finish setting up your account" step that collects role + name and writes the row, which heals all three on next sign-in with no guessing. Needs its own small design; do not bolt it onto a hotfix branch. Size: small.
- **Practice Arena + Brain Gym visual upgrade (activated by Thomas 2026-09-04).** **Current follow-up: SGS and shared 3D scenarios.** Thomas rejected the earlier top-down treatment. The review candidate adds authored facing/sticks, navy/gold helmets, 3D guided/coach/SGS views with camera adjustment, state-linked question wording and a 1v1–5v5 positioning prototype plus U7 rink discovery. 640 draft configurations of two families are measured separately from physics, AI and curriculum approval. The co-planning document is `docs/one-on-one/SGS-PLAN.md`; actual limits and checks are in `docs/one-on-one/sgs-visual-review.md`. Remaining priorities: coach calibration of one complete family, source-pinned evaluation, production character assets, physical iPad checks and migration of remaining legacy scenario views. Earlier release history follows. See `docs/one-on-one/all-scenario-visuals.md` and the September 5 all-scenario plan. Earlier deployed work follows: Integrate RinkReads concepts, source questions (including MC/TF), personas and animated plays with Play, Read & React and Coach Lab. The current framework includes connected U9/U11/U13 reads, support routes, actual-branch recall, U11 changed-cue comparison, 12 coach examples, 24 guided lessons and Coach Lab player-route authoring with scalable teams, freeze, preview and save/reopen. **U13 and the connected-read 3D view are live in `cf96277`, verified at 05:34 Edmonton:** four U13 Shoot/Carry paths retain separate possession/loose-puck teaching; procedural navy/gold skaters and goalie use canonical positions, portrait end-zone and landscape broadcast cameras, 44 px targets, tap/numeric movement and actual WebGL-loss fallback to the Tactical board. Recall and static comparison views remain SVG. All 15 age/target paths passed locally at 390 px, with pause, route, scroll-cancellation and exact fallback/save/reload checks. Final 320 px grid/target-clamp fixes, 243 practice tests including ten visual tests, and the production build pass. Live receiver touch, route/numeric input, reduced-motion inspection, exact reflection/download/reload and recall opening passed while preserving three prior U9/U11 records. Brain Gym preserves all twelve games, with upgraded Shootout/Best Option visuals, accurate introductions and shared readability fixes. Preview: https://ice-iq.vercel.app/review/; live evidence: `docs/one-on-one/phone-preview.md`. Design/evidence: `docs/one-on-one/u13-lane-switch-design.md`, `docs/one-on-one/connected-read-visual-design.md` and `docs/one-on-one/morning-review.md`. Practice records stay on the device; no new AI judgment, physics or child-comprehension validation is claimed. Broader overnight work continues from `docs/one-on-one/overnight-checkpoint.md`. The procedural art is a prototype, not finished rigged production assets. No paid assets purchased. Spec: `docs/superpowers/specs/2026-09-04-rinkreads-practice-framework-expansion.md`; plan: `docs/superpowers/plans/2026-09-04-practice-framework-expansion.md`.
- **Act on the source-triage report.** Full run done 2026-07-19: 114 transcripts assessed, **8 PURSUE** — headline: Ken Martel constraints-led approach (Tier 1, USA Hockey ADM), Pavel Barber puck protection, Kim Weiss wall-play scanning, Gulutzan D at the blue line, off-puck "availability" podcast. Report: `docs/factory/coach-runs/source-triage-2026-07-20.md`. Every PURSUE is flagged **re-acquire via the authenticated TCS manifest before citing** — feeds the LATER research-library workstream's manifest+auth step. Next: pull the 8 into the Obsidian acquisition log (wave 1) and start extraction.
- **Scenario-engine foundation — Phases 0-6 SHIPPED, engine defect CLOSED.** Built and committed to `feature/shareable-beta` on 2026-07-31 (`2ec3b20`..`a98c411`): P1 canonical schemas/RinkFrame/presentation adapters, P2 sourced physics profiles + Level-1 deterministic simulator, P3 decision evaluation + compiled playback + shared clock, P4 machine-readable tactical claims store + validator, P5 immutable factory run envelope + breakout family + coach-declared fixture, P6 state machine + judgment records + promotion + recall. `npm run test:scenario-engine` green. Overnight autonomous scaling stays disabled. Verified live 2026-08-01 against real Supabase rows: provenance hashing holds end to end, the finalize gate correctly refuses an impossible skate with a specific cited-capability reason, and the export worker renders a real 1920×1080 MP4 (watermarking unvalidated drafts unconditionally, confirmed on the pixels). ~~**Open engine defect — the validated motion is not the motion that plays.**~~ **CLOSED 2026-08-03.** Both halves, and the entry above was stale on the first one. **Acceleration** was already fixed 2026-08-01 (`level1-simulator-v4`): `sampleAction` samples the constant-acceleration-from-rest curve the detectors certify, and `detectImpossibleSampledAcceleration` measures the emitted trace rather than the declared endpoints. Verified by measuring a real trace, not by reading the changelog — the skate track ramps 0.15/0.45/0.75/1.05/1.35/1.65 m per interval, with pass legs correctly still linear. **Speed** was never checked anywhere and is now (`22e49ac`, decision 1 of round 4): `topSpeedMPS` was cited in every profile and read only for stopping distance and interception timing, so nothing asked whether a skater's own motion stayed under it — and the from-rest model creates that gap, since a leg exits at `v = 2d/T`. A 40 m route over 8 s certifies at 1.25 m/s² against a 4.13 cap and exits at 10.0 m/s. `detectImpossibleSpeed` recovers the peak from the interval midpoint identity (exact under constant acceleration, independent of sample step) rather than reading an interval average, which understates it. It caught a real one on its first run, in the engine's own turning fixture. The guard test asserts sampled peak speed equals the validated model's `2d/T` on every leg, so the two cannot drift apart again. Remaining phases: 9 (compliant scheduled-runner proof, Windows task stays Disabled throughout) and 10 (honest throughput benchmark) — **both now unblocked**.

## 🟢 NEXT — sequenced, in order

1. **Scenario/kernel engine foundation and scalability. UNPARKED by Thomas 2026-07-29, gated by the approved foundation design and a reviewed implementation plan.** *(2026-08-03: the engine defect that gated Phases 9-10 is closed — see NOW.)* Phase 1 shipped 2026-07-21: `src/play/kernels/twoOnOneKernel.js` (correct-by-construction answer/geometry) + `src/play/noveltyGate.js` + staging report. The measured baseline is 48 candidates → 4 novel survivors, so throughput is not yet proven. **Phases 0-6 are now shipped** (see the NOW entry for the commit range and what each delivered): breakout fixture isolated/hashed, scenario/trace/playback bundle, sourced physics profiles, schema-validated tactical claims, the immutable run/event/dependency model, Claude judgment, conservative promotion, and recall are all built and test-covered. Remaining: Phase 9 (scheduled-runner safety, task stays Disabled) and Phase 10 (throughput benchmark). Throughput benchmark: measure directly once those land, no longer gated on the coach-authoring MVP (that item is parked — see Parking Lot). Canonical spec: `docs/superpowers/specs/2026-07-29-scenario-engine-design.md`. Prior kernel spec: `docs/superpowers/specs/2026-07-21-play-kernel-engine-design.md`. Art-QC design: `docs/superpowers/specs/2026-07-21-art-qc-autolearning-design.md`. Size: multi-stage; benchmark, do not assume throughput. **This is the current focus (Thomas, 2026-08-01): the core play-diagram/quiz review experience, no coach-authoring or team-portal work layered on top until this ships.**
2. **Bulk batch 002 through the kind-aware factory. BLOCKED by NEXT #1 acceptance gates.** Regenerate family coverage before choosing targets; the July 21 counts are stale. When unblocked, run the standard capped batch via `check:bulk` and the batch-plan template. Every new play must use the validated timing/physics path; there is no legacy-path exception. The kernel ready tray (`npm run report:kernel-expansion` → `docs/factory/kernel-expansion-report.md`) remains evidence/staging, not proof that its candidates are physics-clean or promotion-ready. Size: days after foundation.
3. **Native rebuild of the scenario-inspiration backlog (animated plays).** Work `docs/scenario-inspiration/native-rebuild-plan.md` Phase 3 in order — gap-control step-up, backcheck weak-side trailer, off-puck support, then the U9/U7 concepts — one play per commit with regenerated reports, built on the upgraded engine (curves/beats/anchors/trails). Includes upgrading take-away-the-reverse to true behind-net reverse geometry now that curves exist, and the flagged visual fix for pre-answer "middle protected / middle outlet denied" labels in the two older plays. Untracked expansion batch stays inspiration-only; never `git add .`. Pair with the player-facing integration now underway. Size: days.
4. **Cycle 2 — Daily Faceoff (arcade shell v1).** First game mode: 5 curated reads/day per band, calendar streak, earned "Backup Goalie" streak protection (never purchasable; "paused" not "lost"), milestone celebrations at 3/7/14/30. Session wrapper over the existing catalog, no new rink primitives. Spec → plan → build. *Owns the calendar streak — gym Phase 2 waits on this so the streak has ONE owner.* Size: weeks.
5. **Parent/coach weekly progress card v1.** Days played, accuracy by concept/family, streak state, printable milestone badges. Telemetry is already kind-aware, so the data exists — this is rendering + delivery. Ships alongside/right after Daily Faceoff so the streak has an adult witness. Size: days.
6. **Brain Gym Phase 2 — Today's Practice + Rink Rating.** One-button daily workout (3 auto-picked drills: weakest domain, strongest, rotating third; ~5–8 min; hard stop with rest framing), Rink Rating headline score over 6 hockey-named domains with a Rink Map radar, Rookie Combine placement flow, 6 starter drills + 5 earned unlocks, staircase tuned to ~85% success. *Blocked-by: NEXT #4 (Daily Faceoff owns the calendar streak; Today's Practice credits it, never a second streak).* Spec: Phase 2 section of `docs/proposals/2026-07-09-cognitive-gym-overhaul-design.md`; plan written when its turn comes. Size: weeks.

## ⚪ LATER — in scope, not yet sequenced

- **Evidence-led curriculum and research library.** Build an Obsidian-first, tiered source and evidence library, including a Research Library hub, reusable templates, Bases views, research-gap notes, and a crosswalk to existing work. Retain original documents in Google Drive and export only validated product data to IceIQ. Include permission-aware capture from Thomas's authenticated coaches' site; extract evidence into an adaptive skater curriculum. Curriculum admission requires documentary support plus coach review; player testing validates comprehension and transfer. Planning brief: `docs/superpowers/specs/2026-07-10-evidence-led-curriculum-research-library-design.md`. **Progress 2026-07-10:** Obsidian workspace built (Research Library hub, six templates, `Research/` folders + acquisition log, five Bases dashboards, task-roadmap mirror) in `SecondBrain\Command Center\Projects\RinkReads\`. Remaining: confirm Google Drive folder, crosswalk seed inventory, public-source pilot, coaches'-site manifest + auth, wave 1 acquisition.
- **Skip a question and come back.** Requested by Thomas 2026-08-03 after being trapped on a broken question with no way past it: a defective or confusing question currently makes the whole session unfinishable, which is far worse than the defect itself. **DECIDED by Thomas 2026-08-03: a skip counts WRONG, and the question stays available to come back to.** So the result is recorded as incorrect immediately (no free pass, no farming a perfect score by skipping the hard ones) but the question is re-queued rather than consumed, and answering it later REPLACES the provisional wrong result. That replacement is the fiddly part: `results` is keyed by position, not question id, so revisiting has to update the existing row rather than append — appending is exactly the double-record bug fixed on 2026-08-02. Original framing: Excluding it is the honest reading (the player made no read) but it lets a player farm a perfect score by skipping anything hard, so a cap or a "skipped" tally on the results screen probably has to come with it. Note the denominator is `results.length`, which is shared with `calcWeightedIQ()` — the same array that caused the 2026-08-02 scoring bug — so whichever way it goes, it has to be decided at the point the result is recorded, not patched onto the display. Size: small once the call is made.
- **"How to play" first-run walkthrough, interactive rather than text.** Requested by Thomas 2026-08-03. The right granularity is **per interaction kind, not per game** — there are five primitives (`point` tap-a-spot, `selection` tap-a-player, `place` drag-and-check, `path` draw-a-route, `sequence` order-the-steps) and each has genuinely different mechanics. A player meeting `place` for the first time has no way to know the tokens are draggable, which is part of why "I can't drag the players" was ambiguous between a bug and a UI gap (it turned out to be a real bug, but the ambiguity is the point). Suggested shape: first time a player meets a kind, a short guided overlay that has them perform the action once on a throwaway example before the real question starts, then never shows again for that kind (per-player flag). Ties into the existing `youngQ`/`youngT` age-banded copy — U7–U11 need this more and need it wordless. Size: small-to-medium per kind, and they can ship one at a time.
- **Rule-legality awareness in content (offside, icing).** Raised by Thomas 2026-08-03 on the Brain Gym "Best Option" drill: an option can be tactically sensible but illegal, and the question has to be mindful of that. This is the second live instance of the gap identified 2026-08-01 — **no validator anywhere checks whether a depicted or chosen play is legal**; every gate we own checks physically-possible, visually-clean, novel, or sourced. Note it is surface-conditional: Hockey Canada half-ice U9 has no offside or icing at all, so any check keys off the surface model (also parked). **DECIDED by Thomas 2026-08-03: teach offside rather than avoid it.** The Best Option generator should deliberately produce positions where a pass is available but illegal, making CARRY the right read *because* the pass is offside. That converts the defect into the only content we have that teaches the rule, and needs a fourth feedback line explaining why. See `docs/factory/2026-08-03-best-option-positioning.md`. Cheapest first step is still a content review pass over options involving passes across a blue line. Size: unknown until the review happens.
- **Consequence chains (two-read plays). DEFERRED by Thomas 2026-08-02** — playtest backlog first; finding and fixing what is already broken beats adding a mechanic on top of it. Design, agreed in conversation and recorded here so it is not lost: **the branching engine already exists and is 100% unused by content.** `AnimatedPlay.jsx` already does `setNodeId(opt.next)` and every play already routes different answers to different outcome nodes (`dz_breakout_escape_pressure` has six). What is missing is only that those outcome nodes are `terminal: true` — the consequence *ends* instead of asking again. A chain is therefore **pure data, no engine work**: drop `terminal: true` from an outcome node, give it an `ask`, point its options at two new terminals. Agreed shape: **2 reads, branches converge** — promote two outcome nodes (one after a right read, one after a wrong one) into second decisions that arrive at the same question from different circumstances, 3 decision nodes per play, fixed length so scoring and telemetry stay simple. Scope was 4 retrofits, U13+ only (U7–U11 keep single reads; the `youngQ`/`youngT` copy exists because that band needs less load). Authoring rule: the second read must be a genuinely different decision — if the wrong branch's second read is obvious *because* you know you erred, it is a scold with extra steps. **Verify before authoring:** whether `validateFactoryStandards` / `artLint` / the decision-richness gate assume one decision node per play, and how scoring handles it — the latter matters more now that `results` turned out to be shared with `calcWeightedIQ()`. Size: days, mostly content.
- **Surface model / age-appropriate ice personalization.** Plays declare a surface (`cross-ice` / `half-ice` / `full-ice` / `zone-local`), players and teams declare the surface their program plays on, and serving filters on the match. Design + the correction that reshaped it: `docs/superpowers/specs/2026-08-02-surface-fit-validator-design.md`. **Deferred 2026-08-02, no live defect to fix:** the catalog was measured and its largest footprint across all 25 plays is 55ft x 44ft, so every play already fits both a U9 half-ice surface (100x85) and a U7 cross-ice area — `space.units: "rink-200x85"` is the coordinate frame, not a footprint. Hockey Canada has mandated cross-ice at U7 and half-ice at U9 since 2017-18, and our content already complies without modification (worth knowing commercially). The open question if this resumes is **conceptual, not geometric**: whether a read *depends* on full-ice structure (a blue line to gain, a long backcheck) even at a small footprint — `play_backcheck_recovery_defender_gets_beat_u13_v1` is the test case. That needs a coach's judgment, not code. Also unbuilt and noted here so it isn't lost: no validator anywhere checks rule-legality (offside, positional legality), which is only meaningful on full ice since Hockey Canada half-ice U9 has neither offside nor icing.
- **Play-engine backlog (from the 2026-07-09 audit)** — token facing, camera punch-ins on the freeze, puck-follows-path on curved passes, reveal audio/particles, headless still exporter for batch QC, seeded variation. Revisit after the first native batch ships. `docs/play-engine-audit.md`.
- **Multi-step Phase 2: gauntlet generation** — teach the gauntlet visual creator to GENERATE multi-step plays, decision-richness gate per step. Own spec/plan. (from backlog)
- **Rework single-option seeds (triage theme 1)** — decision-richness gate flags 7 seeds as one-option with concrete rework ideas: `gvis_u11_reading-the-play_b633`, `u13_nz_regroup_hinge_v1`, `u13_gap_pivot_match_mc_v1`, `u13_gap_stepup_mc_v1`, `u13_dz_breakout_rim_v1`, `u11_dz_breakout_center_support_v1`, `u13_gap_pinch_forecheck_mc_v1`.
- **Goalie-reading concept (triage theme 4)** — new ledger concept/node + goalie-state representation (set vs moving), then scenarios. Natural multi-step use case.
- **U7 time-and-space support content** — `u7_time_space_open_ice_mc_v1` to teach where to go to support (time + space). U7 stays generic-players (settled).
- **step_index telemetry persistence** — `MultiStepPlayer` emits `stepIndex` but persisting needs a Supabase migration (add `step_index` column, default 0). Emitted but ignored downstream until then.
- **On-deck game modes** — Rush Hour (time attack, needs the gentle timer), young-age DRAG mini-games (Cover the Pass, Set the Forecheck), mascot pass (the sound pass shipped with gym Phase 1).
- **Brain Gym Phase 3 — age-banded identity layer.** U7–U11 "Practice Rink" (near-zero text, ghost-hand demos, no fail states, sticker collectibles, station path) + U13+ "The Combine" (athlete dashboard: trends, personal bests, Backup Goalie streak repair) + hockey-native stimulus swaps (Shoot or Hold light → net-opens vs ref's-arm-up; Two Things shapes → coach's bench signals; Baylor's Pick ⚽🐰🐻 → pucks/jerseys). Rides with the parent/coach card (NEXT #5) and the mascot pass. Spec: Phase 3 section of `docs/proposals/2026-07-09-cognitive-gym-overhaul-design.md`.
- ~~**Pick Your Spot — first-person shootout redesign.**~~ **SHIPPED 2026-07-21** as "Shootout": first-person breakaway (net + goalie grow on the skate-in, the approach is the shot clock), NHL-shootout scoreboard + shot pips, per-session goalie tendency with an honest scouting report (weighted coverage in `shootoutCore.js`, tested), forgiving-aim tap snap, win/lose end card. Re-registered in the Brain Gym lineup (same `shootout` id, player records persist). Also shipped the same session: engine-level combo/bestCombo + hot-streak audio escalation for all drills, `mirrorPlayY` far-side play variants (3 wired into the catalog), coach-feedback headline pools 4+4 → 12+12, and **Run the Play** (drill 12, Simon-style sequence memory with per-skater tones — closes the Memory-domain gap the overhaul spec flagged). Spec: `docs/superpowers/specs/2026-07-21-shootout-first-person-redesign.md`. Needs Thomas's visual playtest on a phone before it counts as done-done.

## 🅿️ PARKING LOT — out of current scope (captured, not sequenced)

- **Send the beta link to real users** — LAUNCH/distribution. Smoke pass is done + auth confirmed working; this is a timing/channel judgment call, not a build task. Out per current scope.
- **Marketing / distribution of RinkReads** generally — out per current scope.
- **Original physics-based arcade hockey game — activated into NOW on 2026-09-04.** The approved practice-rink slice and subsequent integrated RinkReads framework are now active work (see NOW). Full commercial hockey simulation, production character/motion assets, validated team tactics and actual iPad performance remain open; this does not promote prototype outcomes into curriculum truth.
- **Coach-created animated play/video MVP (PARKED by Thomas 2026-08-01).** Was NEXT #2; built, code-reviewed, and final-review-clean on the unmerged branch `feature/coach-authoring-video-export` (worktree at `.worktrees/coach-authoring-video-export`), with a live-verification checklist (`docs/handoffs/2026-08-01-coach-authoring-live-verification-checklist.md`) still awaiting Thomas's walkthrough. Parked, not abandoned: the branch/worktree are untouched, no destructive action taken. Known open items if this resumes: declared/derived agreement is currently tautological (single-candidate, not independently corroborated), `contentHash` freezes at v1 (never recomputed after first save), finalized rows are still deletable at the RLS layer (only the client hides the control), and a newly-found rendering defect (the puck circle is drawn on top of and hides the actor in every exported video, `CoachPlayComposition.jsx`). Do not resume without re-reading the checklist's "What you are actually looking at" section first — the UI is thinner than earlier task-list wording suggested (no animated preview, no in-app Export button, raw unstyled HTML controls).
- **Team Communication Portal MVP (PARKED by Thomas 2026-08-01, same session it was approved).** Two-feature design (team announcements + event calendar/RSVP) approved fast-tracked earlier the same day; never built, no branch created. Full design: `docs/superpowers/specs/2026-08-01-team-communication-portal-mvp-design.md`. Parked before any implementation started.
- *(New ideas land here first, then get promoted into NEXT/LATER on purpose.)*

## Changelog

- **2026-09-05** — Prepared packets10–11 (45 independently rechecked question versions) and the migration-independent player release from exact576e1c1/e6c5c05/78194fa file changes. App uses576e1c1, not8428bf7. Supabase, coach, training and goal-builder files remain at production baseline. 73 tests and build passed; sample Home/library and U15 discovery suppression verified at390px.

- **2026-09-05** — Prepared approved packet-only deployment from production f1f4306, carrying the reviewed 02–09 content, immutable receipts, catalogs and exact question manifest. Packet01 calibration was already deployed. Separate player-home, training, coach and unfinished rendering work excluded. No promotion to human-approved content or mastery.

- **2026-09-05, 16:36 Edmonton — Claude calibration repairs:** original return preserved, all 50 records validated, five scenarios updated with 28 exact independent Luna rechecks. A remaining duplicate rim task became a W-support placement; the proposed goalie-contact rewrite was declined. Current combined review has zero unreconciled AI flags, 1,600 matching hashes and preserved historical findings. 34 focused tests and the isolated build pass; selected 390/1280 px placement, feedback and reload checks pass. Public publishing remains blocked by automatic approval review; no deployment claimed.

- **2026-09-05, 16:14 Edmonton — scoped practice UI release:** assembled the exact release from HEAD without sweeping mixed player/coach/engine WIP. Added worlds and second-level journeys, experimental practice/workshop/report, optional reflection persistence and compact number-only navigation, focus/full views, shaded area labels and puck legend. Repaired age/lesson/concept links, stale review receipts, draft carryover and answer input during player drags. Preserved the original world art and old-game animation rig. Isolated suite: 437 passes; build passes; 390/1280 px browser checks include save/reopen and exact-hash draft export. Evidence: `docs/one-on-one/2026-09-05-practice-ui-release.md`.

- **2026-09-05, 15:03 Edmonton — all rink-background Brain Gym games use 3D:** converted nine remaining rink games using the canonical rink, shared player rig, broadcast camera and coordinate-preserving taps. The two previously 3D games remain; the reaction game has no rink. Review repaired target/number leaks, feedback tolerances, pass timing, the missing Late Read arrow and stray Run the Play order labels. Runtime checks exercised recall, timed cues, taps and feedback. Exact scoped game checkout: 377 practice tests and build pass; full local integration: 561 tests pass. A concurrent run stalled in the unchanged judge timeout test; the isolated serial rerun passed. Shared renderer staging includes only the Arena visibility change needed by these games. Physical devices and all level combinations remain unverified.

- **2026-09-05, 14:45 Edmonton — expanded bank, repairs and Claude operating contract:** completed the requested +1,000 questions with exact age counts, independent Luna review and hash-bound repair rechecks. Preserved historical findings, including an explicitly superseded geometry adjudication. Created a portable Claude project with 40 five-scene packets, full source/curriculum context, repair history, return templates and validators that reject stale or incomplete reviews. Local browser checks confirm the repaired rim, number-only tabs, optional reflection, shaded regions, puck legend, exact-version events and six-world mission journeys. Full local practice suite: 560 passes; catalog/return suite: 13 passes; build passes. Human approval, production integration of mixed WIP, semantic curriculum bindings and physical-device validation remain separate.

- **2026-09-05 — additional 1,000 questions / Claude handoff in progress:** owner chose mixed expansion (100 new scenarios ×6 +100 existing scenarios ×4). Added append-only composition, version/collision and import checks, an offline searchable catalog, source/JSON/CSV exports and copy-ready Claude instructions. Root rejected generic first drafts and tightened Luna authoring checkpoints before any experimental release. Follow-up of the prior55 flags is recorded in `docs/factory/research/question-review/followup/review.html`; original600 remain unchanged.

- **2026-09-05, 12:28 Edmonton — Experimental bank and critical coaching review:** created the requested age mix (10/15/25/25/15/10) with six questions each. Lower-cost Luna reviewers applied the owner-selected puck-management, fundamentals, time/space and flexible-role rubric; all 600 questions reviewed and all 127 flagged/high-risk cases independently checked. Four concrete wording/answer repairs rechecked; 55 individually reconciled teaching-design items remain in the triage queue. Local question workshop supports exact-version flags and revision drafts without publishing. Added spaced-practice defaults (five questions, five dates, two weeks, seven-day span, 80% accuracy), six-world home, observation-based coach ratings and richer goal/check-in modules. Fresh integration verification is recorded in the audit handoff; no human-coach approval or deployment claim.

- **2026-09-05, 11:32 Edmonton — Template direction clarified:** saved a [3D question scenario design template](../one-on-one/3d-question-template.md). Thomas wants those scenarios preserved and broader gameplay considered only where it fits naturally. No game integration or implementation resumed.

- **2026-09-05, 11:31 Edmonton — Template-only follow-up:** recorded Thomas's direction to keep the liked item as a template for future game integration. Asked which artifact he means; no implementation resumed or template identity assumed.

- **2026-09-05, 11:29 Edmonton — Owner-requested pause:** stopped all three implementation agents and saved the [new-chat handoff](../one-on-one/2026-09-05-paused-handoff.md). Preserved unfinished source, UI, preview, journey, daily Intel, training and voice work with explicit verification limits. Resume only when Thomas requests it.

- **2026-09-05 (mixed reads, named focus and teaching factory)** — Implemented three reproducible factual formats, independent atomic attempt records/archives, Learning/Challenge reveal timing and optional positioning notes. Camera pan/reset and clear boards, a neutral navy/gold shared rig, named focus/puck markers and front-view GPT goalie layers address the visual requests. A separate U11 D1 reflection follows actual F2 reception. Source-bound factory and lesson plans now include foundations/vocabulary, the supplied interactive formats, constraints-led/game-based approaches and conditional cross-sport adaptations. Exact local 1v1/2v2/3v3/5v5 save/reload and fallback checks are recorded in the new review note; production verification remains separate from coach/content approval.

- **2026-09-05 (SGS live verification)** — Vercel completed `86c250c`; public 390 px checks passed for current image hashes, three 1v1 answers with exact reload, U7 on-rink feature selection and preservation of fourteen prior records. No page exceptions or horizontal overflow. Refreshed the U7 screenshot after detecting an offscreen capture artifact; application behavior is unchanged.

- **2026-09-05 (SGS and player presentation)** — Replaced same-facing/no-stick curriculum markers with authored poses and visible equipment; added shared rotatable 3D views, camera-neutral curriculum wording, exact loose-puck adapters and both-event connected replay. Built a separate 640-configuration positioning review catalog from 1v1–5v5 and four-feature U7 rink tour. Browser checks cover actual drag, 3v3 continuation/save/export/reload, 5v5 context-loss fallback and U7 native markers. No draft admission or new AI/physics approval. Planning and measured evidence: `docs/one-on-one/SGS-PLAN.md`, `sgs-benchmark.md`, `sgs-visual-review.md`.

- **2026-09-05 (all-scenario visuals)** — Extended the approved artwork through every current renderer family; rebuilt 37 PNGs and four SVGs serving 133 questions from unchanged authored coordinates. Added read-only picture/board inspection, preserved memory/team cues, and fixed legacy image-target scaling and actor-layer height. 265 practice tests, focused identity/age/coordinate checks and production build pass. Released in `22782f6`; public verification at 06:15 Edmonton confirmed all 41 asset hashes, phone inspection, exact answer reload, eleven unchanged older records and 320/390 px review layouts. Detailed scope and browser evidence: `docs/one-on-one/all-scenario-visuals.md`.

- **2026-09-05 — U13 and connected-read 3D verified live.** [Vercel deployment](https://vercel.com/bluechippeoplestrategies-projects/ice-iq/4vwRTyCS7bHcJXFZoZs9CwPKzbAx) completed for `cf96277`; public 390 px verification passed at 05:34 Edmonton. Reload exposed U13/3D and preserved three prior U9/U11 records. Native F2 touch, a route point at (20.75680072542246, 1.1) from exact (21, 5), numeric (22, -1), reduced-motion inspection and a completed explanation produced a download matching the saved reflection exactly; full reload retained it and opened U13 recall. Tactical board unmounted the canvas and 3D remounted one. No errors, failed requests or horizontal overflow in the checked live flow. Final local 320 px grid/target-clamp fixes passed; 243 practice tests, including ten visual tests, and production build pass. Source, AI, physical-device and production-asset limits remain. Evidence: `docs/one-on-one/phone-preview.md` and `docs/one-on-one/morning-review.md`.

- **2026-09-05 — U13 and connected-read 3D local checkpoint before release.** Implemented four U13 Shoot/Carry paths with branch-specific puck ownership, support origins, recall and age-separated saves; Carry's passing support and Shoot's loose-puck positioning remain distinct, ungraded examples. Added a lazy demand-rendered procedural 3D rink over unchanged canonical states, portrait/end-zone and landscape/broadcast cameras, 44 px target buttons, tap/numeric editing and native context-loss SVG fallback. Static comparison/recall views remain SVG. Production-preview checks at 390 px covered all 15 U9/U11/U13 targets, correct labels/highlight, frozen frames and pause/resume; development touch checks covered route input, swipe cancellation, presentation switching and exact saved-reflection reload after real context loss. No page errors, failed requests or horizontal overflow in checked production paths. 243 practice tests/local build pass. At this checkpoint the final release build and live verification were still pending; their completion is recorded above. No new live AI, physical-device, skating-physics or curriculum-admission claim. Details: `docs/one-on-one/morning-review.md`, `u13-lane-switch-design.md` and `connected-read-visual-design.md`.

- **2026-09-05 — U11 wording verified live.** Vercel completed `2b5ae75`. Public 390 px checks restored the older Shoot reflection and consistent YOU recall descriptions, preserved all three old U9/U11 storage values, and completed/reloaded a fresh Pass/return/support explanation exactly. Target buttons and rink labels agree. No page exceptions, failed requests or overflow in the fresh flow. Evidence: `docs/one-on-one/phone-preview.md`.

- **2026-09-05 — Clearer U11 player questions and recall.** Simplified prompts and consequence descriptions; matched numbered rink targets, buttons, summaries and read-aloud text; aligned recall captions with the YOU marker. Current puck descriptions follow possession without calling every unowned puck loose. Presentation-only changes preserve canonical definitions, branch states, IDs, saved reflections, AI payloads and recall exports. All seven U11 paths passed phone browser checks; production-preview reload/download preserved existing JSON byte-for-byte. 225 practice tests/build pass. Evidence: `docs/one-on-one/u11-player-copy-review.md`. Recorded `docs/one-on-one/u13-lane-switch-design.md` as a proposal only; its Shoot/Carry teaching tradeoff remains explicit.

- **2026-09-05 — Coach routes verified live.** Vercel completed `106ec3e`. The public 390 px review displays 225 tests; native touch/numeric route input, endpoint inspection, Apply/Save/Export/full reload/Reopen preserve exact JSON and endpoint hold. All 3 prior read/recall storage records are unchanged. No failed requests, page exceptions or document overflow in checked live flows. Evidence: `docs/one-on-one/phone-preview.md`. Consolidated the overnight checkpoint to remove superseded next-step instructions.

- **2026-09-05 — Coach Lab player routes.** Coaches can plan 1–12 next positions from a paused player pose using either 3D camera, a full-rink board or numeric coordinates; inspect/animate before Apply; Cancel without changing the draft; and Undo while the applied draft remains current. Remaining selected-player keys are explicitly replaced, earlier motion and other actors are preserved, and the endpoint holds. Native touch scrolling/cancellation and frozen/minimum-time guards are covered. Independent review fixes included phone preview visibility and accurate facing copy. 225 practice tests/build pass; browser checks include touch, nonzero Start, movement, exact export/save/reload and 320/390/1280 px layouts. Pass-transfer authoring and AI route grading remain open. Design: `docs/one-on-one/coach-skating-routes.md`.

- **2026-09-05 — Play recall verified live.** Vercel deployed `8a2a5e9`. The public 390 px review shows 208 tests; an older saved U9 play accepted touch reordering, saved/reopened the note and order exactly, retained Show the order after immediate reload, and left original U9/U11 JSON unchanged. No failed requests, page exceptions or overflow in checked flows. Evidence: `docs/one-on-one/phone-preview.md`.

- **2026-09-05 — Recall the chosen play.** Added optional U9/U11 ordering of exactly three branch-specific freezes, a fixed U9 opening, factual captions, full-size inspection, touch/keyboard moves, read-aloud cancellation, optional note and separate local JSON records/downloads. Original choices, final support and changed-cue comparison are unchanged. Chronology feedback and help use are separate; draft notes survive replay/age changes and fresh attempts clear only their own recall. Nineteen new helper/storage tests cover all eleven paths and 66 permutations. 208 practice tests/build pass; browser flows include mismatch/correction, assistance, unchanged original saves, export/reload and reset isolation. Design: `docs/one-on-one/connected-read-recall.md`.

- **2026-09-05 — Phone read and Coach Lab fixes verified live.** Vercel completed `cabeee0`; the public review shows 189 tests. U9 Carry → pass → coordinate/reason completion restores exactly after reload, preserving the previous U11 save. Reduced-motion replay focuses its visible reflection. Coach coordinate focus stops the clock without replacing the input, Resume retains the frame, and paused Tab leaves the rink. No failed requests, page exceptions or overflow in checked flows. Evidence: `docs/one-on-one/phone-preview.md`.

- **2026-09-05 — Phone read flow and Coach Lab editing.** Put each current question above the rink on stacked layouts and return there after explicit successful decisions, without scrolling during edits or automatic playback. Reduced-motion replay resolves the currently rendered reflection. Coach controls now capture keys only on the focused running rink; Escape pauses, paused Tab works normally, and Resume keeps the current frame. Buffered numeric fields accept negative decimals, ignore incomplete values and pause the director/live clock before typing. Independent review findings fixed. 189 practice tests and build pass, with phone/tablet/desktop browser evidence. Plan: `docs/superpowers/plans/2026-09-05-phone-read-flow-and-coach-input.md`.

- **2026-09-05 — U9 verified live.** Vercel completed `304848c`. The public 390 px flow accepted touch receiver/route input, saved the U9 explanation and restored it on reload while preserving the older U11 saved JSON byte-for-byte. No failed requests, page errors, horizontal overflow or unsupported U9 AI appeared. Evidence: `docs/one-on-one/phone-preview.md`.

- **2026-09-05 — U9 connected reads and age isolation.** Added a source-bound U9 draft with Pass/Carry, two next choices per branch, generic player labels and final support placement/routes. Shared core now resolves the scenario for transitions, storage and restore; old U11 keys and byte-identical branch/reflection/AI outputs remain. Optional device speech stops on phase/age changes. In-memory drafts survive age switches; completed saves/export remain scoped. U9 rejects unsupported AI/comparison. Fixed receiver markers covering their own numbered target and restored direct receiver taps; kept age-picker keyboard focus. 176 practice tests and build pass; all four U9 phone flows plus touch, keyboard, normal/reduced motion, replay and save/reload checked. Plan: `docs/superpowers/plans/2026-09-05-age-scoped-connected-reads.md`.

- **2026-09-05 — Support routes verified live.** Vercel deployed `a046ea6`. The public 390 px Shoot branch accepted a support route, retained the exact route/reason after reload, and exposed no route AI claim. Live forced graphics loss switched Shootout to playable 2D, with the next shot scored. Evidence: `docs/one-on-one/phone-preview.md`.

- **2026-09-05 — U11 support routes and mobile graphics reliability.** Read three now offers a branch-specific route with up to twelve tapped/numeric points, Undo/Clear, preview/pause/progress, reduced-motion manual inspection and saved route/reason alongside the original three reads and changed-cue reflection. Other actors/puck remain frozen; route feedback has no automatic grade or incomplete AI opinion. Blank numeric fields, accidental swipe points and stale routes are handled. Native WebGL loss switches to the existing playable 2D game; completed Shootout/Best Option unmount hidden 3D and remount on restart. 167 practice tests and build pass. Actual touch-event, replay/export/reload, forced context-loss, full-session and remount checks recorded in `docs/one-on-one/verification.md`. Plan: `docs/superpowers/plans/2026-09-05-u11-support-route-and-mobile-fallback.md`.

- **2026-09-05 — Follow-up verified live.** Vercel completed deployment of `98809ec`. The public 390 px review-to-three-reads-to-changed-cue flow preserves original Pass and revised Carry reasons after reload. Updated Gym instructions are visible. No overflow, failed requests or page exceptions in the checked flow. Evidence: `docs/one-on-one/phone-preview.md`.

- **2026-09-05 — Follow-up teaching and readability review.** Added the optional U11 “One thing changes” comparison: only D1 moves from the shaded shot route into the actual puck-to-F2 pass line. The first answer, revised action/reason and original three-read branch remain separate through replay, save/restore and JSON export. Corrected all eleven remaining Gym introductions against their real stimuli; removed unsupported transfer and training-frequency claims without changing scoring. Raised shared secondary-text contrast and used the BlueChip font/colour in text overlays and remaining inputs. Verified reduced-motion sequence flow, phone/tablet comparison, and sample Goals/Skills. 158 practice tests/build pass. Existing original-quiz ALL_AGES_MODE policy remains an explicit age-filtering limitation; the new guided curriculum has separate age scope.

- **2026-09-05 — Public phone link verified.** Vercel completed deployment of `019e8d9`. Live HTTPS review-to-arena/Brain Gym, all four character variants and six-target Shootout passed at 390 px with no overflow, failed requests or page exceptions. Evidence: `docs/one-on-one/phone-preview.md`. Entry: `https://ice-iq.vercel.app/review/`.

- **2026-09-05 — Phone-accessible review.** Owner requested access from a phone. Enabled the existing prototype hashes in production without changing main navigation, isolated hosted practice identity, blocked unconfigured hosted AI calls before network access, and packaged twelve explicit review assets without raw documentation/manifests. Added dependency/link tests; 155 practice tests, production build and 390 px production-preview flow checks pass. Public entry: `https://ice-iq.vercel.app/review/`. This supersedes the earlier DEV-only review restriction.

- **2026-09-05 — Integrated review checkpoint and quality fixes.** Completed the U11 sequence with true pass/carry/shoot branches, target selection, player movement, free reasons, replay and completed-reflection persistence. Integrated 12 coach examples and 24 lessons/48 questions with all 12 source notes mapped. Fixed gold-button contrast, distinguishable radar series, U9 facing controls, canonical goalie positions, profile-scoped persistence, stale saved lists, deferred React errors and keyboard Shootout. Added navy/gold transparent character references and a 40-clip specification, not rigged animation. All independent concrete review findings resolved; 153 practice tests, scenario-engine suite and build pass. Actual browser evidence and remaining work are in `docs/one-on-one/verification.md` and `overnight-checkpoint.md`. The 7am owner review remains the target.

- **2026-09-04 late — Sequence-first and whole-site quality pass.** Owner confirmed U11 three connected reads first, source documents govern, and 7am September 5 review. This supersedes the earlier U13-only/deferred chain restriction in the development preview. Added twelve ready coach questions, complete concept/interaction source map, separate learner/reference comparisons, server-only AI adapter (unconfigured, no live claim), local BlueChip fonts/navy-gold jerseys, and profile-scoped practice saves. Independent review found contrast, chart-series, keyboard-shot and visible-facing issues; fixes and final flow checks are in progress. Checkpoint: `docs/one-on-one/overnight-checkpoint.md`.

- **2026-09-04 — Practice framework owner expansion.** Recorded the approved browser/iPad direction plus subsequent requests to integrate source concepts/questions/personas, add and freeze arbitrary players, author timelines, scale team templates, modernize all Brain Gym games and goalie graphics, and create coherent age-specific curriculum drafts. Added development routes and actual before/after comparisons; preserved live answer keys and staged content boundaries. Current verification and asset costs are recorded under `docs/one-on-one/` and the linked specs/plans.

- **2026-09-04** — Thomas approved tactical claim
  `claim_dz_breakout_retrieval_escape_pressure_v1`. Phase 10.1 now has a
  staged-only vertical slice: a seed-recorded defensive-zone-breakout
  parameter space with three load-bearing axes (forechecker approach, escape
  lane, outlet lane) yields 18 possible U13 ScenarioDefinitions. The suite
  validates each output's schema, approved-claim provenance, novelty, and
  physics. It does not write a live bank, schedule a runner, or promote a
  scenario; the next gate is a capped staged run and a measured throughput
  benchmark. The legacy adapter import-guard test now uses Node filesystem
  scanning rather than a POSIX shell pipeline, so the standard scenario suite
  runs under Windows PowerShell/cmd.

- **2026-08-03 (later)** — Round-4 decisions taken (`docs/manual-playtest/2026-08-03-decisions-round4.md`),
  all four on the recommendation: playback samples the validated acceleration
  curve, Shootout gets a 2D patch and no new dependency, the u13 breakout moves
  its second forechecker rather than re-keying the answer, and Read the Play is
  the next block. Shipped against them: the **engine speed check** (`22e49ac`)
  closing the second half of "the validated motion is not the motion that plays"
  — and correcting this file, which described the already-fixed acceleration half
  as still open. Also shipped earlier the same day: the **QA gate** (`ed46c71`) —
  a goalie behind its own goal line is now an error rather than a third
  ignored warning, and `qa-sweep` exits non-zero above a recorded warning
  baseline instead of exiting 0 whenever nothing was an error; and **Run the
  Play confined to one end zone** (`61136ab`), drawn to real rink geometry with
  no blue line and boards on three sides, which also made it the last of the
  twelve gym drills to get the shared rep count and the Action Rail.

- **2026-08-01** — Thomas parked both the coach-created animated play/video MVP
  (looked at the `feature/coach-authoring-video-export` editor UI live, called
  it not worth polishing right now) and the same-day-approved Team
  Communication Portal MVP, to keep focus on NEXT #1 (the core play-diagram/
  quiz review experience). Neither branch nor spec deleted — see Parking Lot
  for resume state and open items.
- **2026-08-01 (earlier)** — Live verification pass on the coach-authoring
  branch, driven end to end (real Supabase, real browser) before the park
  decision. Passing: migrations 0020/0021 applied and allowlist-gated;
  provenance `contentHash` matches between definition and compiled artifact on
  a real row; the full editor golden path (place actors/puck, draw a route,
  save, reload, reopen unchanged, preview, finalize) with every coordinate
  persisting exactly as clicked; the finalize gate refusing an impossible
  skate with a specific cited-capability reason; the export worker rendering
  a real 1920×1080 h264 MP4; unvalidated drafts watermarked `DRAFT — NOT
  VALIDATED` unconditionally (verified by extracting frames, not by trusting
  the render log); the delete control correctly absent on finalized rows.
  Two defects found. (1) **Engine, survives the park:** the physics gate
  certifies an accelerate-from-rest model while `sampleAction` emits
  constant-velocity samples — the validated curve and the played curve are
  different, and the acceleration detector cannot see it because it only
  reads action endpoints. Recorded against NEXT #1 in NOW. (2) **Surface,
  parked with the feature:** in `CoachPlayComposition.jsx` the puck is drawn
  after the actors at the same radius and (for a puck carrier) the same
  coordinates, so it completely hides the player in every exported video.
  Also confirmed: the `coach-play-exports` Storage bucket has never existed,
  so the signed-URL path has never once run, and `setDraftExportInfo()` has
  zero callers. Full detail:
  `docs/handoffs/2026-08-01-coach-authoring-live-verification-checklist.md`.
  Note: the walkthrough left one permanent finalized row in the live DB
  (`8c310d7f-5655-41c7-9480-9c6197853944`) — finalized rows are immutable and
  client-undeletable by design, so removing it needs the service-role key.
- **2026-07-31** — Scenario-engine Phases 1-6 built and committed to
  `feature/shareable-beta` (`2ec3b20`..`a98c411`), then Phase 7 design and
  Phase 8 implementation plan for coach authoring + video export
  (`b80d29e`..`f0511e8`), with Phases 7-8 themselves built on the separate
  unmerged branch `feature/coach-authoring-video-export` (`b718838`). The
  2026-07-30 entry below describes the plan as unapproved and unstarted; that
  was superseded the next day. Engine suite green throughout.
- **2026-07-30** — Wrote the 11-phase scenario-engine implementation plan (still
  unapproved — planning only). Within already-approved scope: fixed a real
  11.5-unit position-drift bug on the breakout fixture and built
  `validateAnchorFidelity` (point + wall-segment landmarks) so it can't
  recur silently; built and live-proved the gate-8 blind second-pass
  judgment mechanism (`src/scenario-engine/gate8*.js`). Used gate 8 for
  real on a 3-play kernel batch — both independent passes rejected all
  three, demonstrating conservative promotion rather than just designing
  it, and that review surfaced two defects reaching into the *live*
  catalog: the correct answer sat at a fixed button position across most
  of the catalog (2-on-1 family: index 2 of 4, zero variance; 62% of all
  question nodes: index 1) — fixed by shuffling display order per
  question at the render layer, not per-play, so it protects the whole
  catalog and everything added to it later; and `mirrorPlayY()` silently
  skipped two coordinate fields, harmless today but a live corruption risk
  the moment a mirrored play uses them — fixed. Also fixed the breakout's
  remaining icon-proximity guessability gap with a justification-step
  follow-up (four rounds of blind adversarial review, three of which each
  caught something real before it landed clean), a family-classification
  gap (13 of 25 plays were correctly categorized by accident, not by
  design), and 4 live copy bugs. Full detail:
  `docs/factory/2026-07-30-kernel-batch-001-gate8-review.md`.
  Separately, live-inspected the running app and researched how
  comparable platforms (chess.com, Duolingo, Khan Academy) solve the same
  problems before shipping two UI fixes: raw internal category slugs
  ("off-puck-support-offense") replaced with real family names on both the
  Read the Play list and the quiz screen, and an explicit "tap the ice"
  instruction added to tap-zone scenarios, which previously had no
  on-screen answer affordance at all. Full detail:
  `docs/factory/2026-07-30-ui-improvement-pass.md`. Also flagged, not
  fixed: two Supabase RLS gaps found while reading the plan doc (`tier`
  column user-writable; `question_overrides` writable by any signed-in
  user, not owner/coach-gated) — backend/security, outside this session's
  scope, needs Thomas's call. 154 tests green throughout, pushed to
  `feature/shareable-beta` (`ed763b8..42b0555`).

- **2026-07-29** - Completed a current-framework fit audit of the scenario-engine
  foundation and saved a Claude handoff at
  `docs/handoffs/2026-07-29-codex-scenario-engine-foundation-handoff.md`.
  Confirmed the shared-core approach fits, identified direct-write, timing,
  authorization, promotion, runner, and manual-gate conflicts, verified the
  scheduled runner remains disabled, and left the nine owner choices pending
  Thomas's explicit approval.

- **2026-07-29** — Reconciled the Claude handoff and existing Markdown into one approved scenario-engine foundation: machine-readable tactical claims, deterministic hockey physics, timing-faithful playback, immutable run/judgment/recall records, free-only Claude/Ollama roles, honest throughput benchmark, protected coach author/export MVP, and a separate future arcade runtime. Archived the May 2 routing map, corrected live-bank routing, moved foundation + coach MVP ahead of bulk generation, and left the overnight task disabled pending reviewed spec/plan and safety gates.

- **2026-07-20 (overnight)** — Autonomous LATER-item sweep, three closed + triage filed. (1) `feat/board-mc-questions` verified fully merged into main (0 unique commits) — LATER item removed; branch safe to delete when Thomas confirms. (2) Review fast-follows shipped: `resolveKind` returns null for watch nodes (regression test added), factory standards now lint justify copy to the primary-ask bar (full catalog passes), verdict two-step surfaces the judge pick's authored why/no after the justify answer (was dead copy). 48/48 question-kinds, 21/21 play-engine. (3) Sign-in flash root-caused and fixed: the 2s authReady escape timer could fire mid-profile-load and paint the logged-out landing; escape now disarmed the moment a session is known. Code-verified only (needs a real slow-network session to observe). (4) The 8 PURSUE transcripts registered in the Obsidian wave-1 Acquisition Log with next-step (TCS-manifest re-acquisition; Martel CLA priority).

- **2026-07-19** — NOW front cleared in one session. (1) PR #3 merged to `main` (100 commits; Thomas chose merge-now over playtest-first; gym playtest moved to NOW as a live-on-main check). (2) Source triage run for real, all 5 channels: 114 assessed, 8 PURSUE (1× Tier 1 Ken Martel CLA, 1× Tier 2 Gulutzan, 6× Tier 3), report at `docs/factory/coach-runs/source-triage-2026-07-20.md`; all PURSUEs need TCS-manifest re-acquisition before citing. (3) Read the Play tile BUILT + LIVE-VERIFIED: `src/play/ReadThePlay.jsx` (band-normalized `playsForAge`, per-play summary stats, standard animated-play telemetry) + Home tile (U11/U13 only) + `readplay` screen in App.jsx (lazy). Verified in the browser as the U11 demo player: tile → catalog list → 2-on-1 play → correct answer → Coach Danno feedback → back → "1/1 reads". Build green, play-engine 21/21.

- **2026-07-12** — Brain Gym playtest-fix pass (Baylor's Pick, Shoot or Hold, Find the Lane, Best Option, Late Read, Two Things at Once), Read the Numbers rebuilt, `position:fixed` results-screen bug fixed, Pick Your Spot pulled from the lineup (first-person redesign parked). Cognitive Gym renamed to Brain Gym (display text + `.claude/skills/` folder only; `src/cognitive-gym/` left as-is). New `tools/source-triage.mjs` designed, built (7 tasks, task-level + final whole-branch review, all findings fixed), 78/78 tests passing, never run for real. Untracked content-factory files brought under git; scraped raw transcripts deliberately excluded. Pushed; PR #3 opened against `main`. NOW updated to reflect the owner-gate + merge decision and the source-triage go-ahead, both still pending Thomas.
- **2026-07-10** — Built the research-library Obsidian workspace per the handoff: `RinkReads - Research Library` hub linked from the Project Hub; templates for research sources, evidence notes, curriculum concepts, content crosswalk, research gaps, and crawl manifests; `Research/` folder structure with acquisition log (wave 1 targets seeded, not started); five Bases dashboards (Sources, Evidence, Concepts, Crosswalk, Gaps); `RinkReads - Task Roadmap` mirror of this file (this file stays canonical). No Drive folders created (target unconfirmed), no acquisition started, nothing pushed.
- **2026-07-10** — Created a self-contained Claude handoff for the evidence-led curriculum research library, including settled decisions, live paths, Obsidian workspace requirements, authenticated-site rules, implementation sequence, quality gates, and acceptance criteria: `docs/handoffs/2026-07-10-claude-rinkreads-research-library-handoff.md`.
- **2026-07-10** — Added the evidence-led curriculum and research-library planning brief: Obsidian owns research knowledge, including a dedicated hub, templates, Bases views, research gaps, and existing-work crosswalk; Drive owns retained source documents; IceIQ owns executable data and automation. Includes read-only authenticated coach-site capture, competency progression, coach-review admission, and player-transfer validation. Captured in LATER without displacing the active player-facing integration.
- **2026-07-10** — Question-kinds cycle 1 passed owner review. Predict-next wording was grounded in a visible player decision; forced-pass responsibility moved to F1; support-angle became a separate sibling read; interception/counter choreography passed; coach feedback now appears after every answer with bounded language and separated attribution. All five kinds are factory-eligible. Player-facing animated-play integration promoted to NOW.
- **2026-07-09 (evening)** — Play-engine audit + 4 improvements shipped (waypoint curves, choreography beats, outcome ghost trails, rink anchors; new gate `npm run test:play-engine`, 18 tests; all existing gates green). Earlier today: library source docs committed, gap-control pair QC'd (distinct, keep both), forecheck take-away-the-reverse built. Native rebuild of the inspiration backlog promoted into NEXT #3 (per Thomas); engine backlog captured in LATER; gym items renumbered NEXT #5–#7.
- **2026-07-09 (later)** — Gym Phase 1 built, tested, committed (8 gym commits); NOW entry converted to the owner's 10-min gate + push decision. Gym Phase 2 added as NEXT #6 (blocked by Daily Faceoff, one streak owner); gym Phase 3 spelled out in LATER; sound pass marked shipped.
- **2026-07-09** — Created. Folded `2026-07-09-next-7.md` + `docs/BACKLOG.md` into one living doc; archived the dated next-7, session-handoff, and backlog to `docs/roadmap/archive/`. Beta-send moved to Parking Lot (launch, out of scope). Playtest gate + Gym Phase 1 set as NOW.
