# Approved practice UI and calibration deployment

September 5, 2026. Thomas explicitly approved the push after reviewing the two
commit IDs and the public source/content payload.

- UI commit: `292e395796e416227c1567d67899a1281306dbaf`.
- Calibration commit: `f1f430667964fe0e0aeef3ba74bded79d918f9b0`.
- GitHub `main` was verified at the calibration commit after the push.
- [Vercel deployment](https://vercel.com/bluechippeoplestrategies-projects/ice-iq/Ae6kGZ4gynmhXzp4hHzv6Mi6TidR)
  reported `success` with “Deployment has completed” for that exact commit.
- Live app: [RinkReads](https://ice-iq.vercel.app/).

Before the push, the 34 focused content/return/review tests passed again. The
isolated production build passed in 10.21 seconds with the previously recorded
chunk and import warnings. An independent Luna check matched all ten relevant
bank, extension and review-manifest files against the built candidate. The index
was empty; unrelated working-folder edits were not included.

Production checks used a fresh browser context at 390 × 844 with mobile/touch
emulation. The repaired U13 rim q9 selects W and shows the conditional shorter
outlet question. Entering (-16,11), placing the player and comparing the answer
showed the expected coaching feedback. Full reload preserved that position and
feedback. The checked flow had no horizontal overflow or application console
errors. The U13 Rush Arena route displayed all six worlds and seven journey stops,
including the distinction between visiting a stop and mastery.

The live curriculum coverage JSON returned HTTP 200 with the correct JSON content
type and 1,000,337 bytes. SHA-256 matched the approved local artifact exactly:
`05443b3adc7d03ee00837b1fd5fc5dd344c336b7d5c50841661a88b891c78274`.

This record supersedes the earlier “not published” status as deployment evidence;
the earlier calibration verification remains an unchanged record of its local
checks. Human-coach approval, all camera/device combinations, broader player and
coach WIP, and on-ice transfer remain outside this deployment verification.

This deployment record and its task-list update are local documentation only.
They are not an additional app deployment.
