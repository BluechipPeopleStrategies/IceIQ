# Repairs, placement areas and calibration choices

The five exact verified question repairs are now in the source files. The application script checks all before hashes and versions before mutation and verifies all after hashes. The separate application receipt preserves the reviewed identities. Current catalog hashes and the U11 curriculum binding were regenerated; historical review files were not rewritten.

The subsequent editorial pass changes three calibration decision prompts, varies single-choice answer positions and narrows two titles to what a static scene can assess. Four calibration scene versions were advanced; the separate refinement receipt records their new hashes. This is editorial work, not a new independent or human coaching approval. The historical coaching-pilot route intentionally retains its frozen content; use normal experimental practice for the applied retrieval repair and the regular calibration route for refined choices.

Two development-only placement rubrics now support a Show draft areas toggle after submission. The same half-metre grid is rendered in 3D and overhead. A cell is shown only when its centre and four corners satisfy strong or workable checks, keeping boundary cells conservative. Green indicates strong draft geometry; gold indicates a workable range. These are approximate geometric areas, not guaranteed hockey outcomes. Other placement questions keep their ungraded fallback.

First saved and latest checks are compared for the same question/content/rubric identity, using the bounded local event history. Changes hide the feedback and shading until another check. Camera and board changes preserve the answer. The history retains at most 300 recent checks in this browser; it is not durable cross-device storage. No mastery is awarded.

Verification: seven evaluator tests, nine bank tests and three application/identity tests passed. Production build and the feedback release-boundary check passed. Local browser checks confirmed shaded areas in both 3D and overhead and an F2 move from (-16,0) to (-16,4) changing strong to adjust while preserving the first/latest comparison. Build retains existing large-chunk warnings. Full device/touch coverage has not been certified.

Still outstanding: expert calibration of thresholds, family-specific rubrics for the remaining placement tasks, broader bank distractor review, and authenticated shared storage. All 295 live placement questions remain uncalibrated; two have local draft previews. Supabase remains deferred. Nothing was deployed in this pass.
