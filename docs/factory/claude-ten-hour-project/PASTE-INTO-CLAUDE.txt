Execute this RinkReads project until completed. The previous ten-hour limit is removed. Read START-HERE.md and assignment.json, verify the package and execute A-F autonomously. Checkpoint every 30 minutes, but do not stop at checkpoints or elapsed-time milestones. Fix in-scope failures and finish validation, owned commits and the verified return package. If blocked on one task, continue every independent task. Do not ask routine questions. If runtime/context/usage limits interrupt execution, save RUN-STATE.json and RESUME.md with the exact next action; do not call the assignment complete or claim to run after the session ends. Preserve all existing boundaries: isolated worktree, provisional hockey findings, no live-bank writes, push, deployment, spending, Supabase or paused Hockey Authority/3D work. Begin implementation now and continue until the completion gates are satisfied or no useful authorized work is possible.
