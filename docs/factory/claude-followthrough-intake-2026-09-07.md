# Codex intake: Claude followthrough

Reviewed 2026-09-07. Source worktree: `C:/Users/mtsli/IceIQ/.worktrees/claude-ten-hour-followthrough`; inspected HEAD `4185e6e`. Original return preserved. Disposition: partial delivery, not accepted as fully completed or integrated.

## Verified now

- Re-ran `node --test tools/build-full-curriculum-matrix.test.mjs tools/coaching-feedback-plugin.test.mjs`: 22 passed, 0 failed.
- Prior `curriculum-drafts.json` actually contains 10 questionDrafts. The earlier 12-draft claim was incorrect. Do not manufacture two drafts to hide this discrepancy.
- The matrix loads experimental-bank, legacy-live-bank, scenario-engine-seed and pov-questions. It does not load the animated-play catalog. Its shared domain-signal helper explicitly describes keyword signals as inventory aids, not authored bindings.
- The report admits browser entry-point/mobile/persistence checks were not executed and a duplicate-submission issue remains unfixed. RUN-STATE nevertheless marks D and the overall assignment complete.
- The integration manifest names 1fbe7ab as finalCommit, while the inspected branch has subsequent commits including 4185e6e. Treat finalCommit as a content checkpoint until the manifest explicitly explains its coverage and reconciliation.

## Required completion work

1. Reopen gate D and overall status. Reproduce the duplicate submission issue in a disposable inbox; determine intended retry semantics and implement a minimal fix if it is a real defect. Preserve distinct legitimate notes. Add a regression test that exercises concurrent requests, not just the client click guard.
2. Run the actual Leave a thought UI through submission, admin disposition and matching-player status. Use a separate browser profile/process to avoid a shared browser lock; never terminate another worker's browser. Cover mobile focus, errors and reload persistence. Record unavailable checks as blocked, not complete. Direct requests do not replace UI checks.
3. Narrow the skating headline to the inventories and classification methods actually checked. Inspect animated-play routes and any other reachable content before asserting product-wide absence. Separate dead/unreachable, pending/retired, unbound and live content. Tests for zero mapped rows do not establish semantic absence.
4. Reconcile completion of all 10 real drafts, retaining their provisional/schema-partial status. Keep source and geometry limitations explicit. No content promotion.
5. Refresh the integration manifest with current file hashes and clear content-commit versus receipt-commit semantics; verify the final return copy. Preserve original failed findings and their retractions.

## Limits of this intake

This was a report/code-path intake plus the two test suites above. Codex has not rerun the browser workflow, independently judged all drafts, validated every screenshot or certified the complete matrix. No bank changes, merge, push or deployment occurred. The original qualification and paused-workstream boundaries remain.

## Copy to Claude

Continue your existing followthrough branch using this intake as the remaining queue. The assignment is not fully complete: reopen D, correct the coverage overclaim and reconcile the return manifest. Execute the five completion items above until resolved, with evidence and resumable checkpoints. Preserve the original return and report fixes as new revisions. No live-bank writes, push, deployment, spending, Supabase or paused Hockey Authority/3D work.
