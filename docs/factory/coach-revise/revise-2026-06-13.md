# Coach Auto-Revise — 2026-06-13

12 board(s).

**Tally:** flagged 3 · applied 7 · applied-marked 2

## gvis_u11_reading-the-play_b633 — flagged ⚠
- change: Moved puck to the point; repositioned opp_corner to the near post and opp_slot drifting across the crease, creating two genuinely competing net-front threats; correct answer changed to near-post player; updated prompt, feedback, tip, and why to match the new puck-at-point scenario.
- warnings: selection answer "opp_corner" has a defender in its lane from the carrier — the "open" read isn't actually open.
- note: hard errors after retry — left flagged

## gvis_u11_time-and-space_4we8 — flagged ⚠
- change: Moved d2 from (0.74, 0.42) to (0.68, 0.22) to credibly threaten the t1/LW area, so both options appear contested and an expert must weigh which player has more time and space rather than spotting an obviously open vs obviously covered read.
- errors: every selectable teammate's passing lane is blocked by a defender — the board is unsolvable, there's no open receiver to pick
- warnings: selection answer "t1" has a defender in its lane from the carrier — the "open" read isn't actually open.
- note: hard errors after retry — left flagged

## u11_dz_breakout_center_support_v1 — flagged
- change: Promoted MC block to primary interaction (kind: selection); dropped the point tap that had only one viable tap target, making it impossible to read wrong.
- errors: selection scenarios need at least 2 candidates in interaction.from (got 0) — otherwise it's not a choice; selection.correct.ids must be a non-empty array of candidate ids
- note: hard errors after retry — left flagged

## u13_nz_regroup_hinge_v1 — applied
- change: Added a center teammate (C at mid-lane) and a second defender (d2 closing the mid-lane pass) so two options require genuine weighing before the player can commit to the safe D-partner hinge; also neutralised the telegraphing prompt by removing 'keeps clean possession'.

## u13_oddman_pass_mc_v1 — applied
- change: Stripped shade-direction conclusion from prompt and stem; shifted defender to y=0.35 to make the visual shade unambiguous; replaced weak 'covered on the left' decoy with genuine shoot and drive-through options so an expert must briefly weigh the read; updated wrong feedback to cover all new options.

## u13_oz_entry_trailer_branch — applied
- change: Stripped the D's commitment from the start-node prompt and mc.stem so the player must read it from the visual; renamed opt 'Shoot far pad right now' to 'Take the shot yourself'; replaced the remedial 'forced' node with a 'mirror' node (D sags to y=0.58, toward the trailer) where the correct answer flips to shoot — completing the 2-on-1 decision tree; updated the else-route next and outcome to flow naturally into the mirror node.

## u13_oz_entry_trailer_v2 — applied-marked ⚠
- change: Replaced theme 'delay' with 'support' (canonical vocabulary, fixes both frames); added defender d2 at (0.78, 0.62) in step 0 between YOU and RW to visibly block the RW passing lane so the wrong selection is geometry-evident rather than concept-recall — LD lane and correct C lane are untouched. Step 2 execution-vs-anticipation labeling deferred: schema has no per-step nodeId override field and the coach called it acceptable as-is.
- warnings: frame[0]: themes outside the controlled vocabulary: support — use the canonical names so filtering keeps working; frame[1]: themes outside the controlled vocabulary: support — use the canonical names so filtering keeps working

## u13_oz_highslot_mc_v1 — applied
- change: Reassigned nodeId from u13.scanning to u13.cycle (no scan mechanic present); moved YOU actor from (0.66, 0.3) to (0.74, 0.65) so the player starts mid-low OZ and moving to the high slot is a genuine spatial choice; rewrote mc.opts[0] to remove 'open' and 'quick scoring pass' so the correct answer cannot be inferred from option text alone; updated mc.stem to drop 'high forward' since YOU no longer starts in that position.

## u13_oz_structure_place_v1 — applied
- change: Moved weakD from completed net-front position (x=0.84) to in-transit (x=0.79) so the player must anticipate the drop rather than observe a finished action; net-front is now a genuine competing destination, resolving the single-option board; updated stemLabel, read.cue, prompt, and feedback to match the in-transit framing

## u13_oz_winger_wall_tf_v1 — applied
- change: Converted to pure point/tap interaction (Path A): updated interaction.prompt to remove all T/F framing, set mc to null, stripped T/F language from feedback while preserving author voice.

## u7_time_space_open_ice_mc_v1 — applied
- change: Replaced mc opts 2 and 3 with genuine U7 traps (support-the-carrier instinct and freeze/hesitation instinct) while keeping opt 0 as the correct answer and opt 1 unchanged

## u9_dz_positioning_v1 — applied-marked ⚠
- change: Fixed goalie y 0.50→0.27 (shooting-angle geometry); loosened place tolerance 0.07→0.10 for U9; added read.cue/decoy so puck-chase wrong spot is surfaced; replaced non-canonical theme 'defensive-side-positioning' with 'dz-coverage' and dropped 'net-front' (check is not within 0.16 of net, scenario works without it)
- warnings: prompt says net-front/crease but the puck (x=0.13, y=0.90) is depth 0.05 from the net — it's not at the net.; goalie "g" at (0.05,0.27) isn't in either crease — verify position; themes outside the controlled vocabulary: dz-coverage — use the canonical names so filtering keeps working
