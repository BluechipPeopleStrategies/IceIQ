# Open work from the 2026-08-03 playtests

Honest ledger. Thomas asked whether everything touched is finished. **It is not.**
Roughly a third of what was flagged is fixed; the rest is listed here so nothing
lives only in a chat transcript.

Verified against the code where marked ✓, otherwise carried from the audits.

**Update, later on 2026-08-03:** the original next-three are all closed, plus both
round-3 decisions that needed building (Run the Play in-zone, and the QA gate that let
the misplaced goalie through). SMART Goals and Read the Play are now the largest
untouched blocks; `migration_0007` is still Thomas's to run.

---

## Shipped and verified

| Item | Commit |
|---|---|
| Session length frozen; "Question 6 of 5" / 6-of-10 | `f8deb14` |
| WeeklyQuiz `mistake` questions rendered with no ask | `2f08ec5` |
| Colorblind setting applied to every question type + guard | `a8c7f53` |
| Half-wall geometry; 4 boards no longer pass through the goalie | `5e44df9` |
| First-Five data loss: write-through cache, bounded writes, auth clobber | `3eec077` |
| All 156 U11 questions typed | `1056460` |
| `What's Your Next Move?` badge (closes 17 stems) + wording correction | `0616a24` `734167a` |
| Per-coach voices (Marques is the woman) | `fc9f4d3` |
| Zone words gone from U7/U9; 8 colour questions fixed | `bbf5f1f` `a31ec88` |
| Late Read offside: 86–96% → 0%, measured | `2cd4fc7` |
| Content guards + points-curve test | `0f08c4f` |
| Six under-specified clauses, incl. Thomas's own find | `6e93c79` |

---

## OPEN — shell defects, none fixed

- **SHELL-3 · celebration toasts stack and duplicate.** ✓ Verified still open: the
  `for (const m of catMilestones)` loop at [`App.jsx:8355`](../../src/App.jsx#L8355)
  still fires one `toast.celebrate` per milestone with no cap and no dedupe. This is
  what buried the results screen and read as a freeze, plus the `+70 XP` toast
  rendering three times.
- **SHELL-5 · back button destroys quiz progress.** ✓ Verified still open: no
  confirm-on-exit exists. Clicking back mid-quiz silently discards the session.
- **SHELL-7 · placement question timer starts immediately.** No reading grace.
- **SHELL-8 · placement question auto-advanced before it was answered.** Possibly the
  same double-fire class as the 2026-08-02 `onAnswer` bug; never diagnosed.
- **SHELL-9 · a tap-target question with one target cannot be answered wrong.**
- **SHELL-10 · training log has no visible affordance.** Copy says "tap to log a
  session"; no control renders. Plus `+ Log` placement, calendar not auto-opening,
  coach names not persisting.
- **S2-6 · Daily Drill errors out** ("something went wrong"), and reloading dumps you
  to the landing page.

## OPEN — SMART Goals, none fixed

- **S2-1** nothing challenges a weak goal ("better backwards crossovers" passed as
  Specific and Measurable when it is neither)
- **S2-2** no high-level options plus fill-in per SMART field
- **S2-3** three `Save Goal` affordances on one screen
- **S2-4** saving does not navigate back *(the underlying hang is fixed; the
  navigation design is not)*
- **S2-5** no celebration on the first goal

## OPEN — Read the Play, none fixed

S2-9 questions depend on an unspecified teammate action · S2-10 you cannot tell who
you are · S2-11 prompt contradicts the diagram (YOU is F2, not F1) · S2-12 no way
forward, only Replay · S2-13 "Mark this read unclear" is not useful feedback ·
S2-14 angle framing does not match positions · S2-15 only 1 of 2 reads offered ·
S2-16 six player-identification conventions · S2-17 feedback length inconsistent ·
S2-18 unexplained "angle" pill · S2-19 "1 of 1 reads" wording

## OPEN — Cognitive Gym

- ~~**7 of 12 drills** still on old rep counts with no Action Rail~~ — **CLOSED.**
  Six landed in `bbb7a3f`; Run the Play, the seventh, in `61136ab`. Every drill is
  now on `REPS_PER_SESSION` with a rail, and Space fires the rail's one action.
- ~~**Run the Play** — offside/200-foot pass~~ — **CLOSED** (`61136ab`). Decided
  in-zone (decision 1), then built: `drawRink(…, { zone: "end" })` renders one end
  zone to real rink geometry with boards on three sides and no blue line, and
  `makeSkaters` keeps skaters inside it, out of the crease, and off the rail band.
  Rendered in a browser and checked on the pixels.
- **Shootout rendering** — "pretty atrocious". Scoped, options laid out, not decided.
- **Snapshot / Two Things** — partially done; the agent died mid-Two Things and its
  own log was never written.

## OPEN — content

- **27 stems still have no question in them.** ✓ Verified by scan. The badge closed
  the 17 `next` ones; these are the `mc`/`seq` remainder. Each needs its ask added and
  reviewed — this is the A2/A3 batch and it is the largest open content item.
- **`blue line` at U7/U9** — 6 questions held pending a decision (the phrase names a
  rule, not a place).
- **CONTENT-4** `rr-u11-decision-making-4` distractors do not belong to the stem.
- **CONTENT-6** placement feedback never shows where the players should have gone.
- **CONTENT-8** `rr-u11-attacking-1v1-4` image contradicts the prompt.
- **CONTENT-9** scenario layouts inconsistent between question types.
- **CONTENT-12** the stick-blade question needs coach verification.
- **Language batches** — ~27 degendering strings, ~90 Canadian spelling
  normalisations, 113 player-identification strings. Decisions made, sweep not run.

## OPEN — scenario seeds

- **22 violations across 13 seeds** from the coordinate audit. Only the half-wall
  cluster is fixed; the rest stand.
- **`u9_dz_positioning_v1`** — goalie 6.9 ft off its own net centre. Position fixed in
  `06608eb`. **The process finding is now closed too** (`ed46c71`, decision 4): a goalie
  behind its own goal line or >3 m off net centre is an ERROR, not a third warning, and
  `qa-sweep` exits non-zero on any warning above `scripts/qa-warn-baseline.json` — before
  this it exited 0 whenever nothing was an error, so warnings gated nothing. Eight known
  warnings are baselined; a new one blocks. `npm run qa:baseline` re-records.
- **4 of 29 seeds** carry only one of `level` / `levels`, so any tool reading the other
  key mis-bands them.
- **`rinkFeatures.js`** has none of the area names CONTENT-11 asked to teach, and has
  never been used by a question. Prerequisite for the zone-vocabulary content.

## OPEN — infrastructure

- **`training_sessions` does not exist in production.** `migration_0007` is in the repo,
  never applied. Every training-log dual-write has failed silently forever; it survived
  only because that screen writes localStorage first. **Thomas's to run.**
- **Coach voices have nowhere to play.** The mapping is correct, but coach feedback is
  not spoken anywhere — TTS reads questions, not persona lines. The surface does not
  exist.
- **Migrations and production have drifted.** `migration_0022` records five migrated
  tables absent. Worth a full reconciliation, not just the one table.

---

## Next three, in order

~~1. The 7 remaining gym drills~~ · ~~2. SHELL-3 and SHELL-5~~ (`ec9a397`) ·
~~3. The 27 stems~~ (`501ed5a`) — **all three done.** The new next three:

1. **SMART Goals S2-1/2/3/5** — nothing challenges a weak goal, no per-field options,
  three `Save Goal` affordances, no celebration on the first one. Untouched.
2. **Read the Play S2-9 through S2-19** — eleven findings, of which only the forward
  action (S2-12) is fixed. "You cannot tell who you are" and "the prompt contradicts
  the diagram" are the two a player hits first.
3. **Shootout rendering** — scoped, options laid out, still needs Thomas's call before
  it can be built.

## Waiting on Thomas

- ~~Run the Play: in-zone or full-sheet?~~ **Answered** (in-zone) and shipped, `61136ab`.
- ~~`blue line` at U7/U9~~ **Answered** (rewrite around the idea, and match the visuals);
  questions in `6ceacbc`, visuals in `55e2525`.
- Apply `migration_0007` to create `training_sessions`. **Still outstanding** — every
  training-log dual-write has been failing silently since that table never existed.
- Shootout rendering approach, and the Shoot-or-Hold trial count (`avgRt` is noisy at
  5 trials).
